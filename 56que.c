#include<stdio.h>
int main()
{
	int num;
	printf("\n enter a number:");
	scanf("%d",&num);
	if(num>0)
	printf("\nnum is a positive number");
	else
	printf("num is a negative number");
	return 0;
}
